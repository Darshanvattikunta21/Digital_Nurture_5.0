# Git HOL 1 - Repository Setup
This hands-on covers setting up Git configuration and initializing a local Git repository.

## Commands Executed:
1. `git config --global user.name "Your Name"`
2. `git config --global user.email "your.email@example.com"`
3. `git init`
